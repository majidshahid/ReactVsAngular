function Single() {
    return (
        <div>
            <h3>The difference between these is how you import them. If you have a single file with multiple modules, it makes sense to give each module a name and be able to import each of them individually, without having to import the entire contents of the file.</h3>
        </div>
    )
}

export default Single;